import { Text } from "react-native"
import { Container } from "./styles"

const Logout: React.FC = () => {
    return (
        <Container>
            <Text>Logout</Text>
        </Container>
    )
}

export default Logout